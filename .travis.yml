dist: xenial
language: python
python: 3.6.9
install:
  - pip install -r requirements_dev.txt
  - pip install -e .
script:
  - pytest --cov=mpopt
sphinx:
  builder: html
  configuration: conf.py
  fail_on_warning: true
